name: Auto-Generar y Publicar Artículos

on:
  schedule:
    # Ejecutar cada 2 días a las 10:00 AM UTC
    - cron: '0 10 */2 * *'
  
  workflow_dispatch:  # Permite ejecutar manualmente desde GitHub

jobs:
  generate-and-publish:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write  # Necesario para hacer push
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: 📦 Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: 🤖 Generate new article
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          AMAZON_ASSOCIATE_ID: ${{ secrets.AMAZON_ASSOCIATE_ID }}
          AMAZON_ACCESS_KEY: ${{ secrets.AMAZON_ACCESS_KEY }}
          AMAZON_SECRET_KEY: ${{ secrets.AMAZON_SECRET_KEY }}
        run: |
          python scripts/generate_article.py
      
      - name: 📊 Update products database
        env:
          AMAZON_ASSOCIATE_ID: ${{ secrets.AMAZON_ASSOCIATE_ID }}
          AMAZON_ACCESS_KEY: ${{ secrets.AMAZON_ACCESS_KEY }}
          AMAZON_SECRET_KEY: ${{ secrets.AMAZON_SECRET_KEY }}
        run: |
          python scripts/amazon_products.py
      
      - name: 🐦 Share on Twitter/X
        env:
          TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
          TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
          TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_SECRET: ${{ secrets.TWITTER_ACCESS_SECRET }}
          TWITTER_BEARER_TOKEN: ${{ secrets.TWITTER_BEARER_TOKEN }}
          BLOG_URL: ${{ secrets.BLOG_URL }}
        run: |
          python scripts/twitter_share.py
      
      - name: 💾 Commit and push changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add _posts/ productos_amazon.json 2>/dev/null || true
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Auto-generate: New article $(date +'%Y-%m-%d')" && git push)
      
      - name: ✅ Success notification
        if: success()
        run: |
          echo "✅ Article generated and published successfully!"
          echo "📅 Date: $(date)"
          echo "🔗 Site will be updated in a few minutes"
      
      - name: ❌ Failure notification
        if: failure()
        run: |
          echo "❌ Failed to generate article"
          echo "Check the logs above for details"
